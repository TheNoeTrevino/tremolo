import axios from "axios";
//TODO: switch all axios to normal fetch and learn how that shit works

export class UserService {
  async getAllUsers(): Promise<void> {
    try (
    const response = await fetch("http://localhost:5000/students", )
  
}
